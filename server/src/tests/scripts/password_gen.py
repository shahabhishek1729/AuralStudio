import random
import string

word_length = 18
components = [string.ascii_letters, string.digits, "!"]
character = []
for c in components:
    for item in c:
        character.append(item)
        
    
